# Variáveis de Ambiente para Insomnia Collection
# ==============================================
# Configure estas variáveis no Insomnia após importar a collection
# Caminho: Manage Environments > Base Environment ou Local Development

# API Configuration
# -----------------
# URL base da API (já configurada nos ambientes)
# Local Development: http://localhost:3000
# Production: https://api.venturaserra.com

# Authentication
# --------------
# Credenciais de Administrador (pré-configuradas)
ADMIN_EMAIL=admin@local.com
ADMIN_PASSWORD=admin123

# Credenciais de Operador (pré-configuradas)
OPERADOR_EMAIL=joao@local.com
OPERADOR_PASSWORD=123456

# JWT Token (preenchido automaticamente após login)
TOKEN=

# Test Resources
# ---------------
# IDs para testes - obtenha valores reais executando:
# - GET /api/clientes (para cliente_id)
# - GET /api/operadores (para operador_id)
# - POST /api/tracking/register (para codigo_carga)

CLIENTE_ID=
OPERADOR_ID=
CODIGO_CARGA=CRG-2026-001

# Instruções
# -----------
# 1. Importe insomnia-collection.json no Insomnia
# 2. Selecione o ambiente: "Local Development" ou "Production"
# 3. Execute "01. Autenticação > Login" para obter o token automaticamente
# 4. Execute GET /api/clientes para obter um cliente_id válido
# 5. Execute GET /api/operadores para obter um operador_id válido
# 6. Atualize as variáveis cliente_id, operador_id e codigo_carga no ambiente
# 7. Todos os requests usarão essas variáveis automaticamente via {{ _.variavel }}
